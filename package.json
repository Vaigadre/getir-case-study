{
  "name": "getir-case-study",
  "version": "1.0.0",
  "description": "A RESTful API with a single endpoint that fetches the data in the provided MongoDB collection and return the results in the requested format.",
  "main": "app.js",
  "scripts": {
    "start": "export NODE_ENV=local || set \"NODE_ENV=local\"  && node app",
    "test": "export NODE_ENV=local || set \"NODE_ENV=local\" && mocha spec --timeout 10000 --exit"
  },
  "author": "Vaibhav",
  "license": "ISC",
  "dependencies": {
    "@hapi/joi": "^17.1.1",
    "dotenv": "^10.0.0",
    "express": "^4.17.1",
    "mongoose": "^5.13.8"
  },
  "devDependencies": {
    "chai": "^4.3.4",
    "mocha": "^9.1.1",
    "supertest": "^6.1.6"
  }
}
